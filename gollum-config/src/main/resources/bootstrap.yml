server:
  port: 3377

spring:
  application:
    name: gollum-config
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml

#gollum-config-dev.yml