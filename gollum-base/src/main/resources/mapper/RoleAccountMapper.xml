<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dangerousarea.gollum.dao.RoleAccountMapper">

    <insert id="create">
        insert into pms_role_account (application_code, role_id, account_id, data_status, create_time)
        values (#{applicationCode}, #{roleId}, #{accountId}, default, current_timestamp);
    </insert>

</mapper>