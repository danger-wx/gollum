<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dangerousarea.gollum.dao.RoleMapper">

    <insert id="create" parameterType="com.dangerousarea.gollum.domain.entities.Role" useGeneratedKeys="true" keyProperty="id">
        insert into pms_role (brand_id, application_code, name, description, inlay, code, data_status, create_time)
        values (#{brandId}, #{applicationCode}, #{name}, #{description}, default , #{code}, default , current_timestamp);
    </insert>

</mapper>